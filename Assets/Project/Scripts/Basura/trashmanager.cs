using UnityEngine;

public class trashmanager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
} 

/*
 * Hay un manager global
 * Este recibe un vector con listas del diseño de la caida y timing y su puta madre
 * Y dentro de de este vector, se habra recibido una funcion para rellenarlo en funcion del tipo de basura
 * el cual se gestion entero en un script mediante herencia
 */
